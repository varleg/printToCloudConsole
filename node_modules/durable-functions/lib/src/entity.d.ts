import { IEntityFunctionContext } from "./classes";
/** @hidden */
export declare class Entity {
    fn: (context: IEntityFunctionContext) => unknown;
    constructor(fn: (context: IEntityFunctionContext) => unknown);
    listen(): (context: IEntityFunctionContext) => Promise<void>;
    private handle;
    private getCurrentDurableEntityContext;
    private destructOnExit;
    private getInput;
    private getState;
    private return;
    private setState;
    private signalEntity;
    private computeElapsedMilliseconds;
}
