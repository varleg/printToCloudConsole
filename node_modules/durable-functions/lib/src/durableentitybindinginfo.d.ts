import { EntityId, RequestMessage } from "./classes";
/** @hidden */
export declare class DurableEntityBindingInfo {
    readonly self: EntityId;
    readonly exists: boolean;
    readonly state: string | undefined;
    readonly batch: RequestMessage[];
    constructor(self: EntityId, exists: boolean, state: string | undefined, batch: RequestMessage[]);
}
