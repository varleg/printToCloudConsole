import { CreateTimerAction, IAction } from "../classes";
import { Task } from "./task";
import { TaskBase } from "./taskinterfaces";
import { TaskSet } from "./taskset";
import { TimerTask } from "./timertask";
/** @hidden */
export declare class TaskFactory {
    static UncompletedTask(action: IAction): Task;
    static SuccessfulTask(action: IAction, result: unknown, timestamp: Date, id: number, completedHistoryEventIndex: number): Task;
    static FailedTask(action: IAction, reason: string | undefined, timestamp: Date, id: number, completedHistoryEventIndex: number, exception: Error): Task;
    static CompletedTimerTask(action: CreateTimerAction, timestamp: Date, id: number, completedHistoryEventIndex: number): TimerTask;
    static UncompletedTimerTask(action: CreateTimerAction): TimerTask;
    static SuccessfulTaskSet(tasks: TaskBase[], completionIndex: number, result: unknown): TaskSet;
    static FailedTaskSet(tasks: TaskBase[], completionIndex: number, exception: Error): TaskSet;
    static UncompletedTaskSet(tasks: TaskBase[]): TaskSet;
}
