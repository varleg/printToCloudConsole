import { IAction } from "../classes";
import { TaskBase } from "./taskinterfaces";
/** @hidden */
export declare class TaskSet implements TaskBase {
    readonly isCompleted: boolean;
    readonly isFaulted: boolean;
    private readonly tasks;
    private readonly completionIndex?;
    result?: unknown;
    exception?: Error | undefined;
    constructor(isCompleted: boolean, isFaulted: boolean, tasks: TaskBase[], completionIndex?: number | undefined, result?: unknown, exception?: Error | undefined);
    yieldNewActions(): IAction[];
}
